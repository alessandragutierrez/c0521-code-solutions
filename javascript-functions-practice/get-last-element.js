/* exported getLastElement */
function getLastElement(array) {
  var arrayLength = array.length;
  var lastElement = arrayLength - 1;
  var getLastElementResult = array[lastElement];
  return getLastElementResult;
}
